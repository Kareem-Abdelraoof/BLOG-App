<%- include("partials/header") %>

<div class="ui container main text segment">
	<div class="ui huge header">
		<%= blog.title%>
	</div>
		<div class="ui top attached">
			<div class="item">
				<img src=<%= blog.img%> alt="there is an image !!" class="ui rounded centered image">
				<div class="content">
				<p>
					<%= blog.body%> 
				</p>
				<span><%= blog.date.toDateString()%></span>
				</div>	
			</div>
		</div>
		<a href="/blog/<%= blog._id %>/edit" class="ui orange basic button">edit</a>
		<form id="edit" action="/blog/<%= blog._id%>?_method=delete" method="post">
			<button class="ui red basic button">
				Delete
			</button>
		</form>
</div>




<%- include("partials/footer") %>